# electron-builder 在 pnpm 默认的嵌套 node_modules 结构下无法追踪深层依赖。
# 使用 hoisted linker 让 pnpm 安装出和 npm 一样的扁平 node_modules，
# electron-builder 就能正常扫到所有 transitive deps（如 parseley、html-to-text 内部依赖等）。
node-linker=hoisted
